import { SearchTableDirective } from './search-table.directive';

describe('SearchTableDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTableDirective();
    expect(directive).toBeTruthy();
  });
});
